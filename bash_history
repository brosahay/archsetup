sudo reboot
reboot
sudo reboot
sudo nano /boot/refind_linux.conf 
pacman -Syu
sudo pacman -Syu
wifi-menu 
sudo wifi-menu 
sudo pacman -Syu
git
pacman -S git
sudo pacman -S git
sudo pacman -S budgie-desktop clutter-gst gst-plugins-bad gst-plugins-base gst-plugins-base-libs gst-plugins-good gstreamer

sudo pacman -S gnome
gdm
sudo pacman -S gdm
sudo pacman -S lightdm
sudo pacman -S lightdm-gtk-greeter lightdm-gtk-greeter-settings 
sudo pacman -S python-setuptools python-pip
sudo systemctl enable lightdm.service 
sudo reboot
sudo mkdir /etc/pacman.d/hooks
xorg
sudo pacman -S xorg
sudo pacman -S weston
sudo netctl start GreenDot
netctl status GreenDot
sudo pacman -S weston
sudo pacman -S nvidia
sudo nano /etc/mkinitcpio.conf 
sudo nano /boot/refind_linux.conf 
sudo mkinitcpio -p linux
sudo nano /etc/pacman.d/hooks/nvidia.hook
weston
sudo pacman -S xorg-server-xwayland
sudo nano /etc/pacman.d/hooks/nvidia.hook 
sudo pacman -S xorg-server-xwayland
sudo reboot
sudo nano /boot/refind_linux.conf 
sudo reboot
weston
sudo nano /boot/refind_linux.conf 
sudo reboot
sudo netctl start GreenDot
sudo pacman -S wayland
sudo pacman -S weston
weston-launch
sudo systemctl disable lightdm.service 
sudo pacman -S gnome
sudo systemctl enable gdm
sudo reboot
sudo netctl start GreenDot
sudo pacman -S networkmanager
sudo systemctl enable networkmanager 
sudo systemctl enable networkmanager
sudo systemctl enable Netw
sudo pacman -S networkmanager-applet
sudo pacman -S network-manager-applet
sudo pacman -S gnome-extras
sudo pacman -S gnome-extra
sudo reboot
cat update_mirrorlist_pacman.sh 
sudo pacman -S reflector 
sudo pacman -S rsync
sudo sh update_mirrorlist_pacman.sh 
sudo sh update_mirrorlist_pacman.sh 
reset
nano update_mirrorlist_pacman.sh 
sudo sh update_mirrorlist_pacman.sh 
nano update_mirrorlist_pacman.sh 
sudo pacman -R web
sudo pacman -S gnome
sudo pacman -R epiphany 
sudo pacman -S firefox
sudo pacman -S fuse-exfat
sudo pacman -S gnome-keyring
sudo pacman -S fuse
sudo pacman -R empathy avahi 
sudo pacman -R empathy
cd workspace/
cat update_mirrorlist_pacman.sh 
cat /etc/pacman.d/mirrorlist
cat /etc/pacman.d/mirrorlist | grep -v "#"
cat /etc/pacman.d/mirrorlist | grep -v "#" | sed "^$d"
cat /etc/pacman.d/mirrorlist | grep -v "#" | sed "$^d"
cat /etc/pacman.d/mirrorlist | grep -v "#" | sed "$^/d"
cat /etc/pacman.d/mirrorlist | grep -v "#"
reset
cat /etc/pacman.d/mirrorlist | grep -v "#"
cat /etc/pacman.d/mirrorlist | grep -v "#"
cat /etc/pacman.d/mirrorlist
cat /etc/pacman.d/mirrorlist.backup 
reset
cat /etc/pacman.d/mirrorlist.backup 
sudo pacman -S bash-completion 
cd .ssh
ls
mkdir .ssh
mv revosftw.githum.io* .ssh/
cd .ssh
mv revosftw.githum.io revosftw.github.io
mv revosftw.githum.io.pub revosftw.github.io.pub
ls
ls -lrt
chmod 755 -R workspace/
yaourt sublime-text
yaourt thunderbird
cd /tmp
wget https://aur.archlinux.org/packages/pa/package-query/package-query.tar.gz
wget https://aur.archlinux.org/packages/pa/package-query/package-query.tar.gz
wget https://aur.archlinux.org/packages/package-query/package-query.tar.gz
sudo pacman -S git yajl
sudo nano /etc/pacman.d/mirrorlist
sudo pacman -S git yajl
sudo rm /etc/pacman.d/mirrorlist.backup 
git clone https://aur.archlinux.org/package-query.git
cd package-query/
makepkg -si
cd ..
git clone https://aur.archlinux.org/yaourt.git
git clone https://aur.archlinux.org/yaourt.git
cd yaourt/
makepkg -si
cd ..
cd ..
cd ~
reset
yaourt -Syu
reset
yaourt openssh
ssh-keygen -t rsa -b 4096 -C "sahay.rohan@iamgod.in"
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/revosftw.githum.io
ssh-add ~/.ssh/revosftw.github.io
sudo pacman -S xclip
xclip -sel clip < ~/.ssh/revosftw.github.io.pub 
cd workspace/github/
ls
ls -lrt
ls -lrt
cd ..
cd mv github/ GitHub
mv github/ GitHub
ls
cd GitHub/
cd ..
mv GitHub/ github
cd github/
ls
git clone git@github.com:revosftw/archsetup.git
cd archsetup/
ls
sudo pacman -S gnome-calendar 
